{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<main class="min-h-screen bg-[#1e293b] flex items-center justify-center px-4 py-12">
  <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-8 space-y-6">
    <h1 class="text-2xl font-semibold text-gray-800 text-center">Email envoyé</h1>

    <p class="text-gray-700">
      Si un compte correspondant à votre email existe, un message vient de vous être envoyé contenant un lien pour réinitialiser votre mot de passe.&nbsp;<br>
      Ce lien expirera dans <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
    </p>

    <p class="text-gray-700">
      Si vous ne recevez pas d’email, vérifiez vos courriers indésirables ou 
      <a href="{{ path('app_forgot_password_request') }}" class="text-[#f97316] hover:underline">
        réessayez
      </a>.
    </p>

    <div class="text-center">
      <a href="{{ path('app_login') }}" class="inline-block mt-4 text-[#f97316] hover:underline">
        Retour à la connexion
      </a>
    </div>
  </div>
</main>
{% endblock %}