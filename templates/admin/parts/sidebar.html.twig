<nav class="bg-gray-800 text-gray-100 w-64 min-h-screen flex flex-col">
    <div class="p-6 text-2xl font-bold border-b border-gray-700">
        Admin Panel
    </div>

    <ul class="mt-6 flex-grow">
        <li>
            <a href="{{ path('admin_dashboard_index') }}" class="block py-3 px-6 hover:bg-gray-700 {{ app.request.get('_route') == 'admin_dashboard_index' ? 'bg-gray-700' : '' }}">
                Tableau de bord
            </a>
        </li>
        <li>
            <a href="{{ path('admin_courses_index') }}" class="block py-3 px-6 hover:bg-gray-700 {{ app.request.get('_route') == 'admin_courses_index' ? 'bg-gray-700' : '' }}">
                Cours
            </a>
        </li>
        <li>
            <a href="{{ path('admin_users_index') }}" class="block py-3 px-6 hover:bg-gray-700 {{ app.request.get('_route') == 'admin_users_index' ? 'bg-gray-700' : '' }}">
                Utilisateurs
            </a>
        </li>
        <li>
            <a href="{{ path('admin_enrollments_index') }}" class="block py-3 px-6 hover:bg-gray-700 {{ app.request.get('_route') == 'admin_enrollments_index' ? 'bg-gray-700' : '' }}">
                Inscriptions
            </a>
        </li>
        <li>
            <a href="{{ path('admin_payments_index') }}" class="block py-3 px-6 hover:bg-gray-700 {{ app.request.get('_route') == 'admin_payments_index' ? 'bg-gray-700' : '' }}">
                Paiements
            </a>
        </li>
        <li>
            <a href="{{ path('admin_students_index') }}" class="block py-3 px-6 hover:bg-gray-700 {{ app.request.get('_route') == 'admin_students_index' ? 'bg-gray-700' : '' }}">
                Étudiants
            </a>
        </li>
    </ul>

    <div class="border-t border-gray-700">
        <a href="{{ path('app_home') }}" class="block py-3 px-6 text-center text-yellow-400 hover:bg-yellow-600 hover:text-white transition">
            Retour au site
        </a>
    </div>
</nav>
