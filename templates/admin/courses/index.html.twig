{% extends 'admin/base.html.twig' %}

{% block title %}Liste des cours{% endblock %}

{% block body %}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <h1 class="text-3xl font-extrabold text-gray-900 mb-8">Liste des cours</h1>

        <div class="overflow-x-auto shadow-md rounded-lg border border-gray-200">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                <tr>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Nom</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Niveau</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Prix (€)</th>

                    {# ↓↓ UNE SEULE COLONNE ETUDIANTS INSCRITS QUI REGROUPE (LastName + FirstName du student) VIA LA METHODE GETFULLNAME CRÉÉ À LA MAIN DANS L'ENTITY ↓↓ #}
                    <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Nombre Étudiants inscrits</th>

                    <th scope="col" class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Statut</th>
                </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                {% for course in courses %}
                    <tr class="hover:bg-gray-100 transition-colors duration-200">
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ course.name }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700">{{ course.level }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700">{{ course.price|number_format(2, ',', ' ') }}</td>


                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700 relative group">
                            {% set studentCount = course.enrollments|length %}
                            {{ studentCount }}

                            {% if studentCount > 0 %}
                                <div class="absolute hidden group-hover:block bg-white border border-gray-200 rounded shadow-lg p-2 mt-1 z-10">
                                    <ul class="text-xs text-gray-700">
                                        {% for enrollment in course.enrollments %}
                                            <li>{{ enrollment.student.fullName }}</li>
                                        {% endfor %}
                                    </ul>
                                </div>
                            {% endif %}
                        </td>




                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                            {% if course.isOpen %}
                                <span class="text-green-600 font-semibold">Ouvert</span>
                            {% else %}
                                <span class="text-red-600 font-semibold">Fermé</span>
                            {% endif %}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="px-6 py-12 text-center text-gray-500 text-sm">Aucun cours trouvé.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
