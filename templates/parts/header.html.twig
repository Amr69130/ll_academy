<header class="bg-[#101C2D] text-white shadow-md">
  <div class="container mx-auto px-6 py-4 flex items-center justify-between">

    <!-- Logo -->
    <div class="flex items-center gap-2">
      <img src="/images/logo.png" alt="Logo" class="h-8 w-auto" />
      <span class="font-serif text-xl font-semibold">LL<br>School</span>
    </div>

    <!-- Navigation -->
    <nav class="hidden md:flex gap-6 text-sm font-medium">
      <a href="" class="hover:text-yellow-400 transition">Accueil</a>
      <a href="" class="hover:text-yellow-400 transition">Cours</a>
      <a href="" class="hover:text-yellow-400 transition">Panier</a>
      <a href="" class="hover:text-yellow-400 transition">Notre équipe</a>
    </nav>

    <!-- Utilisateur connecté -->
    <div class="flex items-center gap-3">
      <span class="text-sm">Bienvenue {{ app.user ? app.user.firstname : 'User' }}</span>
      <img
        src="{{ asset('images/default-avatar.jpg') }}"
        alt="Avatar"
        class="h-8 w-8 rounded-full object-cover border border-white"
      />
    </div>
  </div>
</header>