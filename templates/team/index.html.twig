{% extends 'base.html.twig' %}

{% block title %}Notre équipe{% endblock %}

{% block body %}
  <section class="w-full max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-10">
    <h1 class="text-3xl md:text-4xl font-bold text-center mb-8 sm:mb-12">
      Notre équipe pédagogique
    </h1>
   {# Texte d'introduction #}
    <div class="max-w-4xl mx-auto text-center mb-10 sm:mb-12">
      <p class="text-lg sm:text-xl text-gray-700 leading-relaxed mb-4">
        Découvrez l'équipe passionnée qui fait la force de notre école de langues
      </p>
      <p class="text-base sm:text-lg text-gray-600 leading-relaxed">
        Nos professeurs expérimentés et polyglotes vous accompagnent avec expertise et bienveillance dans votre apprentissage linguistique. 
        Chacun apporte sa propre méthode pédagogique et sa passion pour les langues, créant un environnement d'apprentissage riche et stimulant.
      </p>
    </div>
    {# Carousel Swiper responsive #}
    <div
      data-controller="carousel"
      class="swiper relative pb-14 sm:pb-12 [--swiper-theme-color:theme(colors.blue.600)] [--swiper-pagination-color:theme(colors.blue.600)] [--swiper-navigation-color:theme(colors.blue.600)]"
      data-carousel-options-value='{
        "loop": true,
        "slidesPerView": 1,
        "spaceBetween": 20,
        "pagination": { "el": ".swiper-pagination", "clickable": true },
        "navigation": { "nextEl": ".swiper-button-next", "prevEl": ".swiper-button-prev" },
        "breakpoints": {
          "640": { "slidesPerView": 1 },
          "768": { "slidesPerView": 2 },
          "1024": { "slidesPerView": 3 }
        }
      }'
    >
      {# Wrapper des slides #}
      <div class="swiper-wrapper">
        {% for post in posts %}
          <div class="swiper-slide">
            <div class="bg-white rounded-2xl shadow-lg p-6 h-full flex flex-col justify-start items-center text-center transition-transform duration-300 hover:scale-[1.02]">
              <img
                src="{{ asset('images/' ~ post.image) }}"
                alt="{{ post.title }}"
                class="w-36 h-36 sm:w-40 sm:h-40 md:w-48 md:h-48 object-cover rounded-xl shadow-md mb-4"
              >
              <h2 class="text-lg sm:text-xl md:text-2xl font-semibold mb-2">
                {{ post.title }}
              </h2>
              <p class="text-gray-700 text-sm sm:text-base leading-relaxed">
                {{ post.content }}
              </p>
            </div>
          </div>
        {% endfor %}
      </div>
 {# Navigation Arrows - Positioned outside swiper container #}
      <div class="swiper-button-prev !absolute !left-0 sm:!-left-4 md:!-left-6 !top-1/2 !-translate-y-1/2 !w-10 !h-10 sm:!w-12 sm:!h-12 !bg-white !shadow-lg !rounded-full !border !border-gray-200 hover:!bg-blue-50 hover:!border-blue-300 !transition-all !duration-200 after:!text-blue-600 after:!text-lg after:!font-bold !z-10">
      </div>
      
      <div class="swiper-button-next !absolute !right-0 sm:!-right-4 md:!-right-6 !top-1/2 !-translate-y-1/2 !w-10 !h-10 sm:!w-12 sm:!h-12 !bg-white !shadow-lg !rounded-full !border !border-gray-200 hover:!bg-blue-50 hover:!border-blue-300 !transition-all !duration-200 after:!text-blue-600 after:!text-lg after:!font-bold !z-10">
      </div>

      {# Pagination Dots - Positioned outside and below swiper #}
      <div class="swiper-pagination !relative !bottom-auto !left-auto !right-auto !mt-8 !text-center !z-10"></div>
    
  </section>
{% endblock %}
